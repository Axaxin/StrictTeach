# ============================================
# OpenAI 兼容接口配置（支持智谱 GLM、OpenAI 等）
# ============================================

# 必填：API Key
# 智谱 GLM: https://open.bigmodel.cn/usercenter/apikeys
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# 可选：API 基础 URL（默认为 OpenAI 官方地址）
# 智谱 GLM 使用: https://open.bigmodel.cn/api/paas/v4
# OpenAI 使用: https://api.openai.com/v1（默认）
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：模型名称（默认 gpt-4o-mini）
# 智谱 GLM 推荐模型: glm-4-flash, glm-4-plus, glm-4-air
# OpenAI 模型: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# ============================================
# Cloudflare Workers API 配置
# ============================================

# Cloudflare Workers API 地址（部署后填写）
# 本地开发可留空，生产环境部署后更新为实际 Workers URL
VITE_API_URL=https://strictteach-api.YOUR-SUBDOMAIN.workers.dev

# ============================================
# 访问密码配置
# ============================================

# 访问密码（可选，留空则不启用密码保护）
# 设置后用户需要输入密码才能访问应用
VITE_ACCESS_PASSWORD=your_password_here

