{
  "name": "strictteach-api",
  "version": "1.0.0",
  "description": "StrictTeach Cloudflare Workers API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create strictteach-db",
    "d1:schema": "wrangler d1 execute strictteach-db --file=./schema.sql --remote",
    "d1:schema:local": "wrangler d1 execute strictteach-db --file=./schema.sql",
    "d1:query": "wrangler d1 execute strictteach-db --command"
  },
  "dependencies": {
    "itty-router": "^4.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0"
  }
}
